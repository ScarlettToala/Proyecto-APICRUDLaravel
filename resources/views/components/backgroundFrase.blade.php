<div class="hero-banner" style="background-image: url('{{ asset($image) }}')">
    <div class="hero-content">
        <h1>{{ $title }}</h1>
        <a href="{{ $link }}" class="hero-button">{{ $linkText }}</a>
    </div>
</div>

